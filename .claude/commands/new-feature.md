---
description: Scaffold a new feature module (api, hooks, components, index)
argument-hint: <feature-name> [short description]
---

Create a new feature module for: $ARGUMENTS

Follow the `new-feature` skill. Mirror `src/features/posts/`. Keep every file ≤100 lines.
Finish by running `npm run lint && npm run check:lines && npm run build`.
