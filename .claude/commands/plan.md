---
description: Write an implementation plan into .claude/plans/
argument-hint: <feature-name or task>
---

Write an implementation plan for: $ARGUMENTS

Read the matching spec in `.claude/specs/` if one exists. Copy `.claude/plans/_template.md` to
`.claude/plans/<name>.md` and fill it in: files to create/change (each ≤100 lines), steps, and
verification. Do not write code yet.
