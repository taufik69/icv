---
description: Verify the project follows its rules (lint, line limit, build)
---

Run `npm run lint`, `npm run check:lines`, and `npm run build`.
If anything fails, list each problem with `file:line` and the fix. For oversized files, use the
`refactor-splitter` subagent.
