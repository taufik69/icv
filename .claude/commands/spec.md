---
description: Write a feature spec into .claude/specs/
argument-hint: <feature-name> <what it should do>
---

Write a spec for: $ARGUMENTS

Copy `.claude/specs/_template.md` to `.claude/specs/<feature-name>.md` and fill every section.
Ask me about anything unclear instead of guessing. Do not write code.
