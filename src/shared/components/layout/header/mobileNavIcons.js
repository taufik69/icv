import { BadgeCheckIcon, GraduationCapIcon, HammerIcon, MailIcon, UserIcon, UsersIcon } from '@/shared/components/icons'

// Icon per top-level nav label (mobile menu only).
export const mobileNavIcons = {
  'About Us': UsersIcon,
  'Student Info': BadgeCheckIcon,
  Domestic: HammerIcon,
  International: GraduationCapIcon,
  'Contact Us': MailIcon,
  Portal: UserIcon,
}
